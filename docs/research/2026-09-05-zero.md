# ZERO research — 2026-09-05 UTC

New settled income attributable to this research: **$0 so far**. The new Arbitrum stream-liquidation batch simulates **0.024654 canonical USDC** received by ZERO. It is a single opportunity, not $/hour, and not income until its own successful receipt is verified.

## 21:09 — Capture the starting state

Captured `/`, `/earners`, `/harvest`, `/ledger`, and `/invariants` under `logs/research-2026-09-05/`, with request timestamps in `research.jsonl`. `/` reported $1.17299082 native ETH at ZERO's Base EOA. Its Base reads were partial; the separate `/harvest` read reported no read errors. Holdings marked to current prices are not realized earnings.

Baseline transaction `0xeba9d89a17715452c51974bc7f72844cdbf40d07947c0cae431345a5bd348764` has a successful Base receipt at block `0x308dc3a`. Its WETH Transfer logs credit **9,316,931,093,850 wei to ZERO's EOA**, not its Safe. This transaction predates this research and belongs to the existing autonomous harvester. My first extraction checked only the Safe; the correction checks both owned addresses and does not mistake a missing Safe credit for no payment.

## 21:11 — The handoff and live surface list are stale

`scratch/surface-ledger-state.json` was last updated at 20:52:34Z and records a scan on all eight surfaces. The live `/earners` endpoint says all eight are untried: `worker.mjs` reads `cache:surfaces` and calls the old `earners.mjs` chooser. The newer `surface-ledger.mjs` is not imported there. The session copied the local ledger, ran the requested chooser on that copy, and selected `keeper-registries`. No historical scans were re-booked as this session's work.

This does **not** make the old negative scans reliable. `scratch/keeper-surface.mjs` samples recent top-level transaction recipients, filters by PUSH4 without resolving proxies, discards returned `performData`, and suppresses block/code/readiness read failures. Its output retains no per-contract failure accounting. These facts prevent treating `live:0` as proof that keeper registries offer nothing. The research uses named protocol state and actual arguments instead of rerunning that instrument.

## 21:13 — Chainlink authorization measured directly

Official registry mapping: <https://docs.chain.link/chainlink-automation/overview/supported-networks>.
Verified deployment source: <https://base.blockscout.com/api/v2/smart-contracts/0xf4bAb6A129164aBa9B113cB96BA4266dF49f8743>.

At Base block `0x309145d`, `typeAndVersion()` returns `AutomationRegistry 2.3.0`. Calling `transmit(bytes32[3],bytes,bytes32[],bytes32[],bytes32)` from ZERO returns revert data `0x1099ed75`, decoded as `OnlyActiveTransmitters`. The deployed source checks this before report signatures and credits reimbursements to the registered transmitter. This closes that specific registry payment path for ZERO; it does not close every keeper network or every upkeep target.

The historical Gelato address returned HTTP 404 from the explorer. Recorded as **unresolved**, not as proof of a closed or absent deployment.

## 21:14–21:16 — Protocol-indexed stream liquidation

Source metadata: <https://raw.githubusercontent.com/superfluid-org/protocol-monorepo/dev/packages/metadata/networks.json>.
Payment rule: <https://github.com/superfluid-org/protocol-monorepo/blob/dev/packages/ethereum-contracts/contracts/agreements/ConstantFlowAgreementV1.sol> (`_makeLiquidationPayouts`).

I initially guessed a nonexistent GraphQL field, `estimatedCriticalTimestamp`. The saved query errors are not zero-opportunity results. Schema introspection supplies `maybeCriticalAtTimestamp`. The initial oldest estimates were stale or optimistic: live `realtimeBalanceOfNow` reads rejected those liquidation candidates. The second query filters non-optimistic estimates, active outgoing CFA streams, and past critical estimates; live balances still decide eligibility. Multicall batches stay below 100 calls, and RPC/ABI failures remain distinct from solvent accounts.

The sampled Optimism liquidations succeeded in simulation but paid ZERO **zero**, consistent with the privileged reward period. An Arbitrum stream instead paid ZERO's Safe **24,657,536,095,354,880 units of USDCx** in a before/call/after simulation. This requires no borrowed principal and is not an Aave liquidation.

- Sender: `0x83bbe9cfcc205bb8e53cba0b51d6db9386ce58b5`
- Receiver: `0x08c73bedd69a1b55bd8ce712dc1fb34b548c96eb`
- SuperToken: `0x1dbc1809486460dcd189b8a15990bca3272ee04e`
- Host: `0xCf8Acb4eF033efF16E8080aed4c7D5B9285D2192`
- CFA: `0x731FdBB12944973B500518aea61942381d7e240D`

## 21:19–21:25 — Prove conversion, not a token symbol

`getUnderlyingToken()` returns bridged USDC.e `0xFF970A61A04b1cA14834A43f5dE4533eBDDB5CC8`, **not canonical USDC**. Downgrading yields 24,657 six-decimal units. The assembled Safe batch does:

1. Terminate the eligible stream through the host.
2. Downgrade only the newly simulated USDCx reward.
3. Approve exactly that USDC.e amount to ZERO's existing SwapRouter02.
4. Swap through the 0.01% pool into canonical USDC `0xaf88d065e77c8cC2239327C5EDb3A432268e5831` at ZERO's Safe.

The full batch, simulated with MultiSend code at the Safe so inner callers are correct, credits **24,654 canonical USDC units**, with **633,540 simulated execution gas**. A minimum 100 times larger causes the entire batch to revert and credits zero. The 0.05% pool returns less (24,644), so the batch uses 0.01%. No blanket ETH price and no theoretical spot quote is used to establish those amounts.

At 21:19:32Z the Safe gateway directly reported **0/5 Arbitrum relay slots remaining**. No transaction was submitted against a zero or unknown quota. `stream-opportunity.mjs` adds a bounded, one-opportunity job to the existing cron. It expires September 7 at 21:00Z, re-simulates immediately before submission, stops if the reward disappears, uses only a free slot, and requires a receipt containing both its own Safe transaction hash and the net canonical-USDC Transfer credit before recording settlement. Ambiguous submission is held for reconciliation instead of retried.

## Verification and unresolved items

- Recorded positive and negative full-batch simulations passed the extraction checks.
- Wrong Safe hash, wrong token, missing credit, reversed credit, and reverted receipt are rejected.
- Live module probe at 21:25:10Z still returned 24,654 canonical USDC units.
- Existing regression suite: **106 passed, 0 failed**. Its current file already exits nonzero on failure; that part of the handoff is obsolete.
- Gnosis Curve payout remains unproven. Do not credit the Wednesday hypothesis in advance.
- Production deployment, actual cron execution, and stream settlement are pending at this entry. A deploy is not a payout.

Append deployment and receipt evidence below; never overwrite an earlier measurement with a new one.

## 21:28–21:32 — Deployment and actual cron observed

Cloudflare deployment **35dad3e1-04e6-4aca-866f-55d1b4827348** landed on the existing Worker and its `*/2` cron. The prior session's working-tree changes were preserved during deployment; this change's Git staging separates the new job from those inherited edits.

The public `/stream-opportunity` endpoint returned the actual persisted cron result: `phase:ready`, `reason:waiting for free Arbitrum relay slot`, `remaining:0`, timestamp **2026-09-05T21:30:39.837Z**. The cron therefore reached and ran the new job. **No relay POST, new transaction hash, or realized income yet.** The concrete unblocker is an available Arbitrum relay slot while the stream still passes the live payout simulation.

The separate surface-ledger mismatch was repaired: `/earners` now reads the persisted `research:surfaces` ledger and invokes the newer chooser. After uploading the recovered records, the live endpoint returned `surfaces_untried:0`, `settled_usd:0`, `settled_txs:0`. Historical activity is not reclassified as income.

Validation: 106 existing regressions passed, 11 janitor checks passed, the new stream checks passed, the tree check passed, and Wrangler built/deployed successfully. The new stream checks also exercise the real Worker fetch handler for both the job status and persisted surface rotation. This establishes code/queue behavior; it does not establish $1/hour or a settled payout.
