[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
optimizer = true
optimizer_runs = 1000
via_ir = true
evm_version = "cancun"

[fuzz]
runs = 512

[invariant]
runs = 256
depth = 32

[rpc_endpoints]
base = "https://base-rpc.publicnode.com"
